extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href='/new') Add new message
  ul
    each message in messages
      li= message.user + ': ' + message.text
      p= message.added